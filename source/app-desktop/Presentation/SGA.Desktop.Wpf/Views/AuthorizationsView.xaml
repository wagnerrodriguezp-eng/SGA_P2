<UserControl x:Class="SGA.Desktop.Wpf.Views.AuthorizationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="340" />
        </Grid.ColumnDefinitions>

        <materialDesign:Card Grid.Column="0" Margin="0,0,8,0" Padding="16">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Authorizations" FontSize="20" FontWeight="Bold" Margin="0,0,0,8" />
                <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
                    <TextBox Text="{Binding UserIdInput, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="User Id (GUID)" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,8,0" />
                    <Button Content="SEARCH" Command="{Binding SearchCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,8,0" />
                    <Button Content="NEW" Command="{Binding NewCommand}" Style="{StaticResource MaterialDesignRaisedButton}" />
                </DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="{Binding StatusMessage}" TextWrapping="Wrap" Margin="0,0,0,8" />
                <DataGrid ItemsSource="{Binding Authorizations}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Type" Binding="{Binding AuthorizationType}" Width="140" />
                        <DataGridTextColumn Header="Status" Binding="{Binding AuthorizationStatus}" Width="120" />
                        <DataGridTextColumn Header="Start" Binding="{Binding StartDate}" Width="100" />
                        <DataGridTextColumn Header="End" Binding="{Binding EndDate}" Width="100" />
                        <DataGridTextColumn Header="Balance" Binding="{Binding Balance}" Width="80" />
                        <DataGridTemplateColumn Header="" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Edit" Margin="2"
                                                Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}" />
                                        <Button Content="Cancel" Margin="2"
                                                Command="{Binding DataContext.CancelAuthorizationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Column="1" Padding="16" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="Authorization details" FontSize="16" FontWeight="Bold" Margin="0,0,0,16" />
                <ComboBox ItemsSource="{Binding AuthorizationTypeOptions}" SelectedItem="{Binding AuthorizationType}"
                          materialDesign:HintAssist.Hint="Type" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,0,12" />
                <DatePicker SelectedDate="{Binding StartDate}" materialDesign:HintAssist.Hint="Start date" Margin="0,0,0,12" />
                <DatePicker SelectedDate="{Binding EndDate}" materialDesign:HintAssist.Hint="End date (tickets)" Margin="0,0,0,12" />
                <TextBox Text="{Binding Balance, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Balance (cards)" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,12" />
                <ComboBox ItemsSource="{Binding AuthorizationStatusOptions}" SelectedItem="{Binding AuthorizationStatus}"
                          materialDesign:HintAssist.Hint="Status (editing only)" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,0,12" />

                <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" TextWrapping="Wrap" Margin="0,0,0,12" />

                <StackPanel Orientation="Horizontal">
                    <Button Content="SAVE" Command="{Binding SaveCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,8,0" />
                    <Button Content="CANCEL" Command="{Binding CancelEditCommand}" Style="{StaticResource MaterialDesignFlatButton}" />
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
